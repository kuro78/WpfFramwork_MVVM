<Page x:Class="MVVMFramework.Views.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MVVMFramework.Views" 
      xmlns:controls="clr-namespace:MVVMFramework.Controls;assembly=MVVMFramework.Controls"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      Title="HomePage"
      d:DesignHeight="450" 
      d:DesignWidth="800"
      mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock FontSize="20" Text="{Binding Title}" />
            <TextBlock Text="{Binding Message}" />
        </StackPanel>

        <StackPanel Grid.Column="1">
            <Button Command="{Binding BusyTestCommand}" Content="Busy Test" />
            <Button Command="{Binding LayerPopupTestCommand}" Content="Layer Popup Test" />
            <Label />
            <controls:AmountKoreanControl x:Name="akc" Amount="{Binding Price, Mode=TwoWay}" />
            <TextBlock Text="{Binding ElementName=akc, Path=Amount}" />
        </StackPanel>
    </Grid>
</Page>
